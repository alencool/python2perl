    my @kw_block = qw(if, elif, else, for, while, def);
    my @kw_line = qw(return, break, continue, print, in);            
    my @kw_logical = qw(not and or);
    my @kw_error = qw(del is raise assert from lambda global try
                      class except import yield exec finally pass);    
    my %kw_bool = (True => 1, False => 0);

